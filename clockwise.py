roll = random.randint(1,6)
for _ in roll:
    if (roll==1):
        return
    else:
        choice = input("Roll(r) or Stop(s)?: ")
        if(choice=="r"):
            roll
        else:
            return

